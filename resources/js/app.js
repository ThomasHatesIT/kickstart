import './bootstrap';
// resources/js/app.js

// Import all of Bootstrap's JS
import * as bootstrap from 'bootstrap';

// This line below might already exist, it's for Laravel's backend features.
// It is NOT related to the Bootstrap CSS framework.
import './bootstrap';